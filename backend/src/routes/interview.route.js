// Interview routes removed. This file is a harmless placeholder to avoid import errors.
import express from 'express';

const router = express.Router();

// No interview routes registered. Feature removed.

export default router;
